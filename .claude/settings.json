{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "The tool input was: $ARGUMENTS. If this was a 'git commit' command (not just git add, git status, git diff, etc.), respond with: 'IMPORTANT: A commit was just made. Check if README.md, CONTRIBUTING.md, and CHANGELOG.md need updating to reflect the changes.' If it was NOT a git commit, respond with nothing."
          }
        ]
      }
    ]
  }
}
