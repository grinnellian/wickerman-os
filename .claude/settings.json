{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Skill(update-config:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'git commit' && echo '{\"hookSpecificOutput\": {\"hookEventName\": \"PreToolUse\", \"additionalContext\": \"REMINDER: Before this commit, verify that README.md, CONTRIBUTING.md, and CHANGELOG.md are up to date with the changes being committed. If they need updating, do that first.\"}}' || true"
          }
        ]
      }
    ]
  }
}
